name: Entry list sync

# Temporarily manual-only while we validate a direct ATP / PlayerZone-public
# source. Do not use third-party aggregators as the production hub.
on:
  workflow_dispatch:

concurrency:
  group: entry-list-sync
  cancel-in-progress: true

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - name: Explain hold
        run: |
          echo "Automatic entry-list sync is paused until a direct ATP/PZ-public source is validated."
