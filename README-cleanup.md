# Tournament detail cleanup patch

Changes tournament detail display:
- Shows only rankings, not player/team names, for cut rows.
- Hides Challenger doubles advanced/on-site columns for ATP Tour events.
- Keeps Challenger-specific columns only for Challenger levels.
- Collapses level/week comparison into one line so it no longer shows a separate random-looking "Same" row.

Upload files into the same repo paths and redeploy.
