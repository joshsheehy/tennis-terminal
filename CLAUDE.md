# Claude Code Instructions

Always commit and push directly to the `main` branch. Do not create feature branches or pull requests.

## Links

The production URL is `https://tennis-terminal-production.up.railway.app`. Whenever giving the user API endpoints or action steps, always format every URL as a clickable markdown hyperlink using that base. Never list a bare path — always provide the full URL as `[label](url)`.
